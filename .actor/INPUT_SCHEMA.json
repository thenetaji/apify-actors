{
  "title": "Youtube Music Downloader",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "url": {
      "title": "Video URL",
      "type": "string",
      "description": "The URL of the video to download as song",
      "editor": "textfield",
      "example": "https://www.youtube.com/watch?v=dQw4w9WgXcQ"
    },
    "useProxy": {
      "title": "Use Proxy",
      "type": "boolean",
      "description": "Enable proxy for downloading",
      "default": true
    },
    "proxyConfiguration": {
      "title": "Proxy Configuration",
      "type": "object",
      "description": "Configure proxy settings for this actor",
      "editor": "proxy"
    },
    "convertToMp3": {
      "title": "Convert to MP3",
      "type": "boolean",
      "description": "Enable to convert the downloaded video to MP3 format",
      "default": true
    }
  },
  "required": ["url"]
}